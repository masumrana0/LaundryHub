import LoadingSpinner from '@/Components/Shared/Loading';
import React from 'react';

const Loading = () => {
    return (
        <div>
            <LoadingSpinner/>
        </div>
    );
};

export default Loading;